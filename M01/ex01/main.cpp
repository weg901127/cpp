#include "Zombie.hpp"

int	main( void ){
	Zombie *zb = zombieHorde(10, "jekim");
	//for (int i = 0 ; i < 10 ; i++)
	//	zb[i].announce();
	(void) zb;
	std::cout << "fffffuck" << std::endl;
	return 0;
}
